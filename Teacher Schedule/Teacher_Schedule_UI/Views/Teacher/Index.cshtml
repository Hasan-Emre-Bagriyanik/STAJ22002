@model List<ResultTeachersDto>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Admin/Index.cshtml";
    var count = 0;
}


<div class="container-fluid pt-4 px-4">
    <div class="row g-4">
        <div class="col-12">
            <div class="bg-light rounded h-100 p-4">
                <h6 class="mb-4">Öğretmen Listesi</h6>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th scope="col">#</th>
                                <th scope="col">Öğretmen Adı</th>
                                <th scope="col">Branşı</th>
                                <th scope="col">Haftalık Çalışma Saati</th>
                                <th scope="col">Sil</th>
                                <th scope="col">Güncelle</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <th scope="row">@count</th>
                                    <td><img src="@item.Image" height="50" width="50" /> @item.FirstName  @item.LastName</td>
                                    <td>@item.Branch</td>
                                    <td>@item.WeeklyHours</td>
                                    <td><a href="/Teacher/DeleteTeacher/@item.TeacherID" class="btn btn-outline-danger">Sil</a></td>
                                    <td><a href="/Teacher/UpdateTeacher/@item.TeacherID" class="btn btn-outline-success">Güncelle</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Teacher/CreateTeacher/" class="btn btn-outline-info">Yeni Öğretmen Girişi</a>
                </div>
            </div>
        </div>
    </div>
</div>

